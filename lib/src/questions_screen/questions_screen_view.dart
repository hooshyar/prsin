import 'package:flutter/material.dart';

class QuestionsScreenViews extends StatelessWidget {
  const QuestionsScreenViews({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text('data'),
    );
  }
}
